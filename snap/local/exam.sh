#!/bin/bash

echo "ls -l $SNAP_COMMON"
ls -l $SNAP_COMMON

echo "ls -l $SNAP_COMMON/data"
ls -l $SNAP_COMMON/data

echo "ls -l $SNAP_COMMON/log"
ls -l $SNAP_COMMON/log

echo "ls -l $SNAP_COMMON/tmp"
ls -l $SNAP_COMMON/tmp
